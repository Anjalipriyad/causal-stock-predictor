statsmodels>=0.14.0      # McNemar test, DM test (check if already present)
hmmlearn>=0.3.0          # HMM regime detection (Check 7)
scipy>=1.11.0            # Binomial test (check if already present)
scikit-learn>=1.3.0      # Isotonic regression calibration (check if already present)